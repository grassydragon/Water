export class Bottom {

    constructor() {
        this.positions = new Int8Array([-1, -1, -1, 1, 1, -1, 1, 1]);
        this.coordinates = new Uint8Array([0, 1, 0, 0, 1, 1, 1, 0]);
        this.image = new Image();
    }

}
